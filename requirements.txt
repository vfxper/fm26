# Telegram Football Manager - Production Dependencies (slim for Render free tier)
# Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
email-validator==2.1.0
python-dotenv==1.0.0

# Database (SQLite + async wrapper)
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.19.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
httpx==0.25.2

# Redis client (only the import is needed; run_local.py monkeypatches it
# to use in-memory cache when REDIS_URL is empty)
redis==5.0.1

# Telegram bot (used by /api/webhook handler — imported at module load)
python-telegram-bot==20.7

# Utils
python-multipart==0.0.6
aiofiles==23.2.1
orjson==3.9.10
